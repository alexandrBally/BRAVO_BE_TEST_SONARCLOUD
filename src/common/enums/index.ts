export enum KEY_VALUE_STORAGE_KEYS_ENUM {
  refreshTokenKey = 'refresh_token_',
  userRefreshTokenKey = 'user_refresh_token_',
  adminRefreshTokenKey = 'admin_refresh_token_',
  adminPasswordResetToken = 'admin_password_reset_token_',
}

export enum ADMIN_STATUS_FILTER {
  pending = 'pending',
  superAdmin = 'superAdmin',
  admin = 'admin',
}
